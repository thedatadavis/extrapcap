# Research notebooks

The production research path is CLI-first so every run is replayable in CI.
Exploratory notebooks may be added here, but committed conclusions must be
regenerated from versioned commands and written to reports/.
