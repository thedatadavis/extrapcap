name: Refresh market data
on:
  workflow_dispatch:
  schedule:
    - cron: '0 4 * * 1-5'
concurrency:
  group: extrapcap-ops-writer
  cancel-in-progress: false
jobs:
  refresh:
    runs-on: ubuntu-latest
    environment: paper
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ops
          fetch-depth: 0
      - name: Sync source branch
        run: .github/scripts/sync-ops.sh
      - uses: actions/setup-python@v6
        with: {python-version: '3.11'}
      - run: pip install -e .
      - name: Refresh current Greenlist
        run: python -m extrapcap.universe.cli --output-dir data/universe
      - name: Refresh daily bars for the Greenlist
        env:
          ALPACA_PAPER: 'true'
          ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
          ALPACA_SECRET_KEY: ${{ secrets.ALPACA_SECRET_KEY }}
        run: |
          greenlist=$(find data/universe -name 'greenlist-*.csv' | sort | tail -1)
          python -m extrapcap.data.refresh_cli --greenlist "$greenlist" --benchmark-symbol SPY --days 730
      - name: Commit normalized bars
        run: |
          git config user.name extrapcap-bot
          git config user.email extrapcap-bot@users.noreply.github.com
          git add data/universe data/normalized/bars.csv data/normalized/bars.csv.metadata.json
          git diff --cached --quiet || git commit -m "data: refresh normalized bars"
          .github/scripts/push-ops.sh
